import { FormatoBooleanoPipe } from './formato-booleano.pipe';

describe('FormatoBooleanoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoBooleanoPipe();
    expect(pipe).toBeTruthy();
  });
});
